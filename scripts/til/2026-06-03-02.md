# TIL: TypeScript discriminated unions

_2026-06-03 02:11 UTC_

A union type where each member has a literal field (e.g. `type`) lets TypeScript narrow the type inside if/switch blocks automatically.
